# Minimal QueryOptimizer stub for test compatibility

from enum import Enum

class OptimizationLevel(Enum):
    BASIC = "basic"
    ADVANCED = "advanced"
    MAXIMUM = "maximum"

class QueryOptimizer:
    pass
